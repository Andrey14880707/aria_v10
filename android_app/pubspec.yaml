name: aria_app
description: ARIA — Local AI Assistant for Android
publish_to: none
version: 10.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http: ^1.2.1

  # State management
  provider: ^6.1.2

  # Persistence
  shared_preferences: ^2.3.1

  # Markdown rendering
  flutter_markdown: ^0.7.3

  # Date formatting
  intl: ^0.19.0

  # WebView for Space preview
  webview_flutter: ^4.7.0

  # File picker for uploading files to Spaces
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
